const myName = 'UltiRequiem';

function printinUppercase(n) {
  // console.log(myName.toUpperCase())
  console.log(n.toUpperCase());
}

// const printinUppercase = n => console.log(n.toUpperCase())

printinUppercase(myName);
